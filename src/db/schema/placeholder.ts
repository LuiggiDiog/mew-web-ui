// TODO: Phase 2 — define Drizzle ORM schema here
//
// Example tables to create:
// - conversations (id, title, createdAt, updatedAt, providerId, model)
// - messages (id, conversationId, role, content, createdAt, model)
// - providers (id, name, type, baseUrl, encryptedApiKey, isActive, defaultModel)
// - settings (key, value)
